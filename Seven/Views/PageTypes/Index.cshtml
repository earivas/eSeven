@model IEnumerable<Seven.Models.PageType>

@{
    ViewBag.Title = "Index";
}
<div id="myModal" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" style="display: none;" data-keyboard="false" data-backdrop="static">
<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            @*<button type="button" class="close" data-dissmiss="modal" aria-hidden="true">x</button>*@
                 <h4 class="modal-title" id="myModalLabel"> Pages Type</h4>
        </div>
        <div class="modal-body">
            <div id="modal-content">
                Cargando...
            </div>
        </div>
    </div>
</div>


</div>

<h2>Page Types</h2>

<div class="row">
    <div class="col-md-offset-11">
        <a id="btnNuevo" data-toggle="modal" href="#myModal" class="btn btn-primary btn-sm">Nuevo</a>
    </div>
</div>
<br/>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PageTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PageTypeDescription)
        </td>
        <td>
            <a href="#myModal" class="btnEditar btn-link btn-info" data-toggle="modal" data-id="@item.PageTypeID"><i class="glyphicon glyphicon-edit" style="font-size:18px;"></i>  </a>  Edit |
            @*@Html.ActionLink("Edit", "Edit", new { id=item.PageTypeID }) |*@
            <a href="#myModal" class="btnDetails btn-link btn-info" data-toggle="modal" data-id="@item.PageTypeID"><i class="glyphicon glyphicon-list-alt"  style="font-size:18px;"></i> </a>   Details |
             @*@Html.ActionLink("Details", "Details", new { id=item.PageTypeID }) |*@
            <a href="#myModal" class="btnDelete btn-link btn-info" data-toggle="modal" data-id="@item.PageTypeID"><i class="glyphicon glyphicon-remove" reddelete style ="font-size:18px;" > </i> </a>  Delete |
            @*@Html.ActionLink("Delete", "Delete", new { id=item.PageTypeID })*@
        </td>
    </tr>
}

</table>
@section scripts
{
<script src="~/Scripts/PageType/PageType.js"></script>
}

