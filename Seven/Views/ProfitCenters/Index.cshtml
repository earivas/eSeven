@model IEnumerable<Seven.Models.ProfitCenter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProfitCenterDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProfitCenterDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProfitCenterID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProfitCenterID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProfitCenterID })
        </td>
    </tr>
}

</table>
