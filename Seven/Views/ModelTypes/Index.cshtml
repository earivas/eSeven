@model IEnumerable<Seven.Models.ModelType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ModelTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ModelTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ModelTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ModelTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ModelTypeID })
        </td>
    </tr>
}

</table>
